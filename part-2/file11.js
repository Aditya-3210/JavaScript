/* const add = (x, y) => {
    return x+y;
}
const result = add(4, 5)
console.log(result) */

// if only one line in func then it can also be written like this
const add = (x, y) => x+y;
//same calling lines here

//writing 